name: Coding Roadmap Reference Match

on:
  push:
    branches: [coding-roadmap-reference-match-v0.7.1]
    paths:
      - 'coding-roadmap-shell/**'
      - '.github/workflows/coding-roadmap-reference-match.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Verify reference UI source
        working-directory: coding-roadmap-shell
        shell: bash
        run: |
          set -euo pipefail
          grep -R -n '이어 학습하기' app/src/main/java/com/codingroadmap/app/ui
          grep -R -n 'HeroStillLife' app/src/main/java/com/codingroadmap/app/ui
          grep -R -n '타입은 값의 종류입니다' app/src/main/java/com/codingroadmap/app/ui
          grep -R -n 'EditorialSerif' app/src/main/java/com/codingroadmap/app/ui
          grep -n 'targetSdk = 36' app/build.gradle.kts
          grep -n 'versionName = "0.7.1"' app/build.gradle.kts
          grep -n 'isDebuggable = false' app/build.gradle.kts

      - name: Build release APK
        working-directory: coding-roadmap-shell
        run: gradle --no-daemon :app:assembleRelease --stacktrace

      - name: Android lint
        working-directory: coding-roadmap-shell
        run: gradle --no-daemon :app:lintRelease --stacktrace

      - name: Verify APK
        working-directory: coding-roadmap-shell
        shell: bash
        run: |
          set -euo pipefail
          APK=app/build/outputs/apk/release/app-release.apk
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -name apksigner -type f | sort -V | tail -1)
          "$APKSIGNER" verify --verbose "$APK"
          AAPT=$(find "$ANDROID_HOME/build-tools" -name aapt -type f | sort -V | tail -1)
          "$AAPT" dump badging "$APK" | tee /tmp/badging.txt
          grep "targetSdkVersion:'36'" /tmp/badging.txt
          grep "versionName='0.7.1'" /tmp/badging.txt
          mkdir -p dist
          cp "$APK" dist/CodingRoadmapFresh_v0.7.1_REFERENCE_MATCH.apk
          sha256sum dist/CodingRoadmapFresh_v0.7.1_REFERENCE_MATCH.apk | tee dist/SHA256.txt

      - uses: actions/upload-artifact@v4
        with:
          name: CodingRoadmapFresh_v0.7.1_REFERENCE_MATCH
          path: |
            coding-roadmap-shell/dist/CodingRoadmapFresh_v0.7.1_REFERENCE_MATCH.apk
            coding-roadmap-shell/dist/SHA256.txt
            coding-roadmap-shell/app/build/reports/lint-results-release.html
          if-no-files-found: error
          retention-days: 7
