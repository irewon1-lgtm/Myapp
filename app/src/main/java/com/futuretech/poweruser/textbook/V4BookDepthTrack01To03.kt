package com.futuretech.poweruser.textbook

internal object V4BookDepthTrack01To03 {
    val packs: List<BookDepthPack> = listOf(
        depthPack(
            sectionId = "V1-C01-S01",
            topics = listOf(
                depthTopic("입력·처리·출력은 왜 그렇게 오래 쓰이는 틀인가", "프로그램이 복잡해져도 결국 바깥에서 어떤 값을 받고, 내부에서 상태나 규칙을 적용하고, 결과를 밖으로 내보낸다. 웹 서버의 요청·응답, 함수의 매개변수·반환값, 데이터 파이프라인의 읽기·변환·저장도 같은 틀로 다시 볼 수 있다. 이 틀을 익히면 처음 보는 코드에서도 ‘무엇이 들어오고 무엇이 나가는가’를 먼저 찾을 수 있다."),
                depthTopic("CPU가 빠른데도 앱이 느릴 수 있는 이유", "프로그램 속도는 CPU 계산만으로 결정되지 않는다. 저장장치에서 파일을 읽는 시간, 네트워크 응답을 기다리는 시간, 메모리가 부족해 데이터를 자주 옮기는 시간, 화면을 다시 그리는 시간도 포함된다. 따라서 느린 앱을 볼 때는 ‘CPU가 느리다’라고 단정하지 말고 어느 단계에서 기다리는지 나눠 봐야 한다."),
                depthTopic("RAM이 부족하면 실제로 무슨 일이 생기는가", "RAM은 실행 중 필요한 데이터를 빠르게 두는 공간이다. 여유가 줄어들면 운영체제는 사용하지 않는 앱을 종료하거나, 일부 데이터를 더 느린 저장장치 쪽으로 내보내거나, 메모리를 확보하기 위해 추가 작업을 한다. 그래서 저장공간이 많이 남아 있어도 RAM이 부족하면 앱 전환이 느려지거나 앱이 다시 시작될 수 있다."),
                depthTopic("실행된다는 것과 맞게 동작한다는 것은 왜 다른가", "문법이 맞아 프로그램이 실행돼도 요구사항을 잘못 표현하면 결과는 틀릴 수 있다. 세금 계산식에서 0.1을 더해야 할 곳에 곱하거나, 할인 조건을 10만원 이상이 아니라 초과로 작성하면 앱은 멀쩡히 실행되면서 잘못된 값을 낸다. 그래서 개발자는 실행 성공 뒤에도 예시 입력과 경계값으로 결과를 검증한다.")
            ),
            workedExample = """
                메모 앱에서 저장 버튼을 누름
                1) 입력: 사용자가 저장 버튼 터치
                2) 처리: 현재 메모 내용을 읽음
                3) 처리: 저장 가능한 형식인지 확인
                4) 처리: 파일 또는 DB에 기록
                5) 출력: 저장 완료 표시

                느리다면 어느 줄에서 기다리는지 나눠 본다.
                터치 처리? 파일 쓰기? DB 잠금? 화면 갱신?
            """,
            mistakes = listOf(
                "RAM 용량과 사진·영상 저장공간을 같은 것으로 생각한다.",
                "프로그램이 실행됐으니 계산 결과도 맞다고 생각한다.",
                "앱이 느리면 무조건 CPU 성능 문제라고 결론낸다.",
                "입력값이 잘못돼도 프로그램이 알아서 의도를 보정해 줄 것이라 생각한다."
            ),
            questions = listOf(
                "함수도 입력·처리·출력으로 볼 수 있을까?",
                "네트워크 요청은 처리 중 어느 구간에서 가장 오래 기다릴까?",
                "운영체제는 여러 프로그램이 동시에 CPU를 쓰게 어떻게 나눌까?",
                "실행은 되지만 결과가 틀린 버그를 어떻게 빨리 찾을까?"
            )
        ),
        depthPack(
            sectionId = "V1-C01-S02",
            topics = listOf(
                depthTopic("파일 이름과 파일 내용은 왜 별개인가", "파일 이름과 확장자는 사람이 관리하기 편하도록 붙인 표식이다. 실제 내용은 저장장치에 기록된 byte들이다. image.jpg라는 이름을 image.txt로 바꿔도 내부 byte가 자동으로 글자가 되지는 않는다. 프로그램은 확장자, MIME type, 파일 헤더 같은 힌트와 실제 내용 구조를 함께 보고 파일을 해석한다."),
                depthTopic("상대 경로와 절대 경로는 언제 문제가 되는가", "절대 경로는 파일 시스템의 기준점부터 위치를 적고, 상대 경로는 현재 작업 위치를 기준으로 적는다. 같은 ../data.txt라도 프로그램을 어느 폴더에서 실행했는지에 따라 다른 파일을 가리킬 수 있다. 개발 환경에서는 되는데 배포 서버에서 파일을 못 찾는 버그가 생기는 대표적인 이유다."),
                depthTopic("bit와 byte가 코드와 무슨 관계인가", "숫자, 문자, 이미지, 음악 모두 저장할 때는 결국 bit들의 배열이 된다. 개발자는 보통 bit를 직접 다루지 않지만 데이터 크기, 네트워크 전송량, 파일 형식, 정수 범위, 색상 표현을 이해하려면 byte 단위 감각이 필요하다. 예를 들어 1KB 텍스트와 5MB 이미지는 전송 비용과 로딩 시간이 크게 다르다."),
                depthTopic("폴더 구조가 커지면 왜 설계 문제가 되는가", "작은 프로젝트는 파일 몇 개만 있어도 되지만 기능이 늘면 이름 충돌과 의존 관계가 복잡해진다. 그래서 화면, 데이터, 네트워크, 공통 도구처럼 책임을 나눠 폴더를 구성한다. 폴더를 예쁘게 정리하는 것이 목적이 아니라 ‘어디를 고쳐야 하는지 빨리 찾게 하는 것’이 목적이다.")
            ),
            workedExample = """
                project/
                  app/
                    main.py
                  data/
                    users.json

                main.py에서 ../data/users.json을 읽는다고 가정
                실행 위치가 project/app 이면 찾을 수 있음
                실행 위치가 project 이면 같은 상대 경로가 다른 곳을 가리킴

                해결 질문: 현재 작업 디렉터리는 어디인가?
            """,
            mistakes = listOf(
                "확장자를 바꾸면 파일 형식 자체가 바뀐다고 생각한다.",
                "상대 경로는 언제 어디서 실행해도 같은 파일을 가리킨다고 생각한다.",
                "1 byte와 문자 한 글자가 항상 같은 크기라고 생각한다.",
                "폴더 구조를 기능 책임이 아니라 파일 종류만 보고 무작정 나눈다."
            ),
            questions = listOf(
                "파일 형식은 내부에서 자기 종류를 어떻게 표시할까?",
                "운영체제마다 경로 표기 방식이 왜 다를까?",
                "문자 한 글자는 왜 1 byte가 아닐 수 있을까?",
                "프로젝트 구조는 언제 기능별로, 언제 계층별로 나누는 게 좋을까?"
            )
        ),
        depthPack(
            sectionId = "V1-C01-S03",
            topics = listOf(
                depthTopic("Unicode와 UTF-8은 왜 같은 말이 아닌가", "Unicode는 문자에 공통 번호를 부여하는 큰 약속이고 UTF-8은 그 번호를 실제 byte로 저장하는 방법 중 하나다. 같은 Unicode 문자도 UTF-8, UTF-16 같은 다른 방식으로 byte 표현이 달라질 수 있다. ‘문자 번호’와 ‘저장 방식’을 분리해서 생각하면 encoding 문제가 훨씬 쉬워진다."),
                depthTopic("한글은 왜 영어보다 byte를 더 쓸 수 있는가", "UTF-8에서는 자주 쓰이는 영문 ASCII 영역 문자는 1 byte로 표현되지만 한글은 보통 3 byte를 사용한다. 그래서 화면에는 한 글자씩 보여도 저장 크기는 다를 수 있다. 문자열 길이를 byte 길이로 착각하면 DB 컬럼, 네트워크 제한, 파일 크기 계산에서 버그가 생긴다."),
                depthTopic("문자 깨짐은 정확히 어느 순간 생기는가", "파일에 저장된 byte 자체가 망가진 경우도 있지만, 정상 byte를 다른 encoding 규칙으로 해석해 깨지는 경우가 많다. 저장할 때 UTF-8로 만든 byte를 읽는 쪽이 다른 규칙이라고 가정하면 같은 숫자 배열을 전혀 다른 문자로 해석한다. 문제를 찾을 때는 ‘어떤 byte가 저장됐고 어떤 규칙으로 읽었는가’를 따로 확인한다."),
                depthTopic("이모지 하나가 길이 계산을 더 어렵게 만드는 이유", "사용자가 보는 글자 하나와 프로그램 내부의 문자 단위가 항상 일치하지 않는다. 일부 이모지는 여러 Unicode code point가 결합돼 화면에서 하나처럼 보일 수 있다. 그래서 단순한 문자열 길이 값만으로 화면 글자 수를 정확히 판단하기 어렵고, UI 자르기나 입력 제한에서는 사용자에게 보이는 단위를 고려해야 한다."),
                depthTopic("encoding과 암호화는 목적부터 다르다", "encoding은 같은 정보를 저장·전송 가능한 표현으로 바꾸는 규칙이라 누구나 규칙을 알면 되돌릴 수 있다. encryption은 비밀키 같은 권한 없이는 내용을 읽기 어렵게 만드는 보안 목적의 변환이다. Base64처럼 모양이 복잡해 보여도 encoding일 뿐 암호화가 아닐 수 있다.")
            ),
            workedExample = """
                문자: 가
                ↓ Unicode에서 문자 번호를 정함
                U+AC00
                ↓ UTF-8 규칙으로 byte 표현
                EA B0 80
                ↓ 파일/네트워크에 byte 저장·전송
                EA B0 80
                ↓ 받는 쪽도 UTF-8로 decoding
                가

                받는 쪽이 다른 규칙으로 읽으면 글자가 깨질 수 있음
            """,
            mistakes = listOf(
                "Unicode와 UTF-8을 완전히 같은 개념이라고 외운다.",
                "문자열 길이와 byte 크기가 항상 같다고 가정한다.",
                "Base64로 바꾸면 내용이 안전하게 암호화된다고 생각한다.",
                "깨진 문자를 보면 원본 파일부터 손상됐다고 단정한다.",
                "화면에 보이는 이모지 한 개가 내부에서도 항상 한 문자 단위라고 가정한다."
            ),
            questions = listOf(
                "UTF-16은 UTF-8과 무엇이 다를까?",
                "HTTP의 Content-Type에 charset이 왜 붙을까?",
                "DB가 문자열 길이를 문자 수와 byte 수 중 무엇으로 제한할까?",
                "Unicode normalization은 왜 검색과 비교에서 문제가 될까?"
            )
        ),
        depthPack(
            sectionId = "V1-C01-S04",
            topics = listOf(
                depthTopic("프로그램 파일과 process는 왜 구분해야 하는가", "저장장치에 있는 프로그램 파일은 아직 실행 중인 일이 아니다. 실행하면 운영체제가 코드와 데이터를 메모리에 올리고 CPU 시간을 배정하면서 process라는 실행 단위를 만든다. 같은 앱을 두 번 실행하면 같은 프로그램 파일에서 여러 process가 만들어질 수도 있다."),
                depthTopic("운영체제는 왜 중간 관리자 역할을 하는가", "앱이 저장장치, 카메라, 네트워크, 메모리를 제멋대로 직접 다루면 서로 충돌하고 보안도 무너진다. 운영체제는 자원 사용 요청을 받아 권한과 순서를 관리한다. 그래서 파일 접근 권한, 카메라 권한, 백그라운드 제한 같은 규칙이 앱과 하드웨어 사이에 존재한다."),
                depthTopic("compiler와 interpreter는 실제로 무엇이 다른가", "사람이 쓴 소스 코드는 CPU가 그대로 이해하는 형태가 아니다. 어떤 언어는 실행 전에 다른 형태로 크게 변환하고, 어떤 언어는 실행 환경이 코드를 읽어 가며 처리한다. 현실의 언어는 두 방식을 섞기도 하므로 이름을 외우기보다 ‘소스 코드가 실행 가능한 형태로 언제 어떻게 바뀌는가’를 보는 편이 좋다."),
                depthTopic("환경이 다르면 같은 코드가 왜 다르게 동작할 수 있는가", "코드는 같아도 운영체제, 언어 버전, 라이브러리 버전, 환경 변수, 파일 경로, 권한이 다르면 결과가 달라질 수 있다. 내 컴퓨터에서는 되는데 서버에서는 안 되는 문제를 줄이려면 실행 환경을 기록하고 의존성 버전을 관리해야 한다."),
                depthTopic("정수 overflow와 소수 오차도 실행 환경 문제의 한 종류다", "컴퓨터는 숫자도 제한된 bit 안에 표현한다. 너무 큰 정수는 범위를 넘을 수 있고, 0.1 같은 일부 소수는 이진수로 정확히 표현하기 어려워 작은 오차가 생긴다. 금융 계산처럼 정확성이 중요한 곳에서는 자료형과 반올림 규칙을 명시해야 한다.")
            ),
            workedExample = """
                source code
                ↓ 언어 도구가 해석/변환
                실행 가능한 명령과 데이터
                ↓ 운영체제가 process 생성
                메모리 할당 + 파일/네트워크 권한 확인
                ↓ CPU가 명령 수행
                결과 출력

                같은 코드라도 언어 버전·OS·권한이 다르면 결과가 달라질 수 있음
            """,
            mistakes = listOf(
                "설치된 프로그램 파일 자체가 곧 실행 중인 process라고 생각한다.",
                "운영체제 없이 앱이 하드웨어를 마음대로 직접 제어한다고 생각한다.",
                "compiler와 interpreter를 두 개의 완전히 배타적인 세계로 외운다.",
                "내 PC에서 실행됐으니 다른 기기에서도 반드시 같을 것이라 가정한다.",
                "모든 숫자가 컴퓨터 안에서 수학책처럼 무한히 정확하게 표현된다고 생각한다."
            ),
            questions = listOf(
                "thread와 process는 어떤 차이가 있을까?",
                "JVM이나 JavaScript engine은 이 흐름에서 어디에 있을까?",
                "환경 변수는 왜 코드 밖에 설정을 두는 방법으로 쓰일까?",
                "금액 계산에서 부동소수점 대신 어떤 방법을 선택할 수 있을까?"
            )
        ),
        depthPack(
            sectionId = "V1-C01-S05",
            topics = listOf(
                depthTopic("코드를 위에서 아래로 읽는 것만으로 부족한 순간", "처음에는 한 줄씩 읽으면 되지만 함수 호출, 조건문, 반복문이 생기면 실제 실행 순서가 화면의 줄 순서와 달라진다. 그래서 값을 적어 두고 어느 줄이 다음에 실행되는지 손으로 추적하는 연습이 중요하다. 디버거의 step 기능도 같은 사고방식을 도와주는 도구다."),
                depthTopic("syntax error와 runtime error와 logic error를 나누는 이유", "문법 오류는 코드 자체를 해석하지 못하는 문제이고, runtime error는 실행 도중 실제 값이나 환경 때문에 실패하는 문제이며, logic error는 실행은 되지만 원하는 답이 아닌 문제다. 종류를 구분하면 어디부터 확인할지가 달라져 문제 해결 속도가 빨라진다."),
                depthTopic("오류 메시지를 무서워하지 말아야 하는 이유", "오류 메시지는 실패했다는 낙인이 아니라 프로그램이 관찰한 증거다. 파일 이름, 줄 번호, 오류 종류, 호출 경로가 들어 있는 경우가 많다. 메시지 전체를 읽고 첫 원인에 가까운 줄을 찾는 습관을 들이면 무작정 코드를 바꾸는 시간을 크게 줄일 수 있다."),
                depthTopic("작게 바꿔 보고 다시 실행하는 이유", "한 번에 열 줄을 고치면 어떤 변화가 문제를 해결했는지 알기 어렵다. 한 가지 가설을 세우고 가장 작은 수정만 한 뒤 같은 입력으로 다시 실행하면 원인과 결과를 연결하기 쉽다. 이 방식이 나중에 Git commit과 자동 테스트 습관으로 이어진다.")
            ),
            workedExample = """
                원하는 결과: 10

                code
                x = 4
                y = 6
                print(x - y)

                실제 결과: -2

                1) syntax error? 아니오, 실행됨
                2) runtime error? 아니오, 중단되지 않음
                3) logic error? 예
                4) 가설: 연산자가 잘못됨
                5) - 를 + 로 한 줄만 수정
                6) 같은 입력으로 다시 실행 → 10
            """,
            mistakes = listOf(
                "오류가 나면 메시지를 읽기 전에 여러 줄을 동시에 바꾼다.",
                "실행이 끝까지 됐다는 이유로 logic error 가능성을 무시한다.",
                "문제 입력을 계속 바꾸면서 수정 전후 결과를 비교하지 못한다.",
                "에러가 난 줄만 보고 그 값을 만든 앞 단계는 확인하지 않는다."
            ),
            questions = listOf(
                "stack trace는 호출 순서를 어떻게 보여 줄까?",
                "breakpoint를 걸면 무엇을 관찰할 수 있을까?",
                "재현 가능한 최소 예제를 만들면 왜 디버깅이 빨라질까?",
                "테스트는 이 실행→관찰→수정 루프를 어떻게 자동화할까?"
            )
        ),

        depthPack(
            sectionId = "V1-C02-S01",
            topics = listOf(
                depthTopic("문제를 코드로 옮기기 전에 왜 쪼개야 하는가", "큰 요구사항을 그대로 코드로 쓰려고 하면 조건과 예외가 뒤섞인다. 입력 받기, 검증하기, 계산하기, 저장하기, 보여주기처럼 작은 책임으로 나누면 각 단계의 입력과 출력이 명확해진다. 이 습관은 함수와 모듈 설계의 출발점이다."),
                depthTopic("값과 변수 이름을 구분해야 하는 이유", "변수는 값 자체가 아니라 프로그램 안에서 값을 가리키고 다시 사용할 수 있게 붙인 이름이다. age라는 이름에 20이 들어 있다가 나중에 21로 바뀔 수 있다. 이름을 값처럼 생각하면 대입과 변경, 참조 동작이 헷갈리기 쉽다."),
                depthTopic("자료형은 왜 필요한가", "컴퓨터는 10이라는 숫자와 '10'이라는 문자열을 다르게 다룬다. 숫자는 덧셈이 가능하지만 문자열은 이어 붙이기가 자연스럽다. 자료형은 어떤 연산이 가능한지, 메모리에 어떻게 표현할지, 외부 입력을 어떻게 검증할지 결정하는 중요한 정보다."),
                depthTopic("좋은 변수 이름은 왜 단순한 취향 문제가 아닌가", "a, b, x처럼 의미 없는 이름은 코드가 짧아 보여도 나중에 의도를 다시 추론해야 한다. totalPrice, retryCount처럼 역할이 드러나는 이름은 코드 자체가 설명서가 되게 한다. 특히 AI가 코드를 생성해도 변수 이름이 요구사항과 맞는지 사람이 확인해야 한다.")
            ),
            workedExample = """
                요구사항: 성인 2명, 어린이 1명의 총 입장료 계산

                나쁜 시작
                a = 2
                b = 1
                c = ?

                문제를 나눔
                adultCount = 2
                childCount = 1
                adultPrice = 12000
                childPrice = 7000
                total = adultCount * adultPrice + childCount * childPrice
            """,
            mistakes = listOf(
                "요구사항을 쪼개지 않고 긴 식 하나에 모두 넣는다.",
                "사용자 입력 문자열을 숫자로 변환·검증하지 않고 바로 계산한다.",
                "변수 이름과 현재 들어 있는 값을 같은 개념으로 생각한다.",
                "의미 없는 짧은 변수 이름을 지나치게 많이 사용한다."
            ),
            questions = listOf(
                "상수와 변수는 언제 나눠야 할까?",
                "null이나 값 없음은 자료형에서 어떻게 표현할까?",
                "mutable과 immutable 값은 어떤 차이를 만들까?",
                "요구사항을 함수 단위로 자르는 기준은 무엇일까?"
            )
        ),
        depthPack(
            sectionId = "V1-C02-S02",
            topics = listOf(
                depthTopic("조건문은 단순히 if를 외우는 공부가 아니다", "조건문은 현실의 규칙을 true와 false로 바꾸는 과정이다. 회원인지, 재고가 있는지, 결제가 성공했는지처럼 자연어 규칙을 정확한 논리식으로 바꿔야 한다. 경계값인 이상·초과, 이하·미만을 잘못 옮기면 실행은 되지만 정책이 틀린 버그가 생긴다."),
                depthTopic("반복문이 필요한 순간을 어떻게 알아차리는가", "비슷한 코드를 여러 번 복사하고 있다면 반복 구조를 의심할 수 있다. 하지만 무조건 반복문으로 줄이는 것이 목적은 아니다. 어떤 값이 매번 달라지고 언제 멈춰야 하는지 명확해야 안전한 반복이 된다."),
                depthTopic("무한루프는 왜 생기는가", "반복 조건이 언젠가 false가 돼야 하는데 그 조건을 바꾸는 코드가 없거나 잘못된 방향으로 값을 바꾸면 끝나지 않는다. 그래서 반복문을 볼 때는 시작값, 계속 조건, 매 반복마다 변하는 값, 종료 상태 네 가지를 확인한다."),
                depthTopic("사용자 입력은 왜 항상 의심해야 하는가", "사용자는 숫자 자리에 글자를 넣거나 빈 값을 보내거나 예상보다 큰 값을 입력할 수 있다. 입력을 그대로 믿으면 계산 오류나 보안 문제가 생길 수 있다. parsing과 validation을 분리해 ‘형식으로 읽을 수 있는가’와 ‘업무 규칙상 허용되는가’를 따로 검사하는 습관이 중요하다.")
            ),
            workedExample = """
                입력: age = "17"
                1) 문자열을 숫자 17로 parsing
                2) 0 이상 130 이하인지 validation
                3) age >= 19 조건 검사
                4) false → 미성년 처리

                반복 예시
                count = 3
                while count > 0
                  count를 출력
                  count를 1 줄임
                count가 0이 되면 종료
            """,
            mistakes = listOf(
                "이상과 초과를 같은 조건으로 작성한다.",
                "반복 종료 조건을 만들지 않고 값도 갱신하지 않는다.",
                "사용자 입력이 항상 올바른 형식이라고 가정한다.",
                "같은 동작을 복사해 붙여넣고 수정하다 한 곳만 다르게 만든다."
            ),
            questions = listOf(
                "여러 조건을 AND와 OR로 섞으면 어떤 순서로 평가될까?",
                "for와 while은 어떤 상황에서 선택하면 좋을까?",
                "입력 검증 실패는 사용자에게 어떻게 설명해야 할까?",
                "반복 중간에 break나 continue를 쓰면 흐름은 어떻게 달라질까?"
            )
        ),
        depthPack(
            sectionId = "V1-C02-S03",
            topics = listOf(
                depthTopic("여러 값을 묶는 자료구조가 필요한 이유", "사용자가 한 명뿐이면 name과 age 변수 두 개로 버틸 수 있지만 천 명이면 변수 이름 천 개를 만들 수 없다. list, map 같은 구조는 같은 종류의 데이터를 묶고 반복 처리하게 해 준다. 자료구조 선택은 데이터를 어떻게 찾고 바꾸는지와 연결된다."),
                depthTopic("함수는 코드를 줄이는 도구보다 책임을 나누는 도구다", "함수로 묶는 진짜 이유는 같은 코드 복사를 줄이는 것뿐 아니라 한 가지 일을 이름 붙여 독립적으로 생각하기 위해서다. calculateTotal, validateEmail처럼 역할을 분리하면 테스트와 재사용이 쉬워지고 버그 범위도 좁아진다."),
                depthTopic("parameter와 return을 흐름으로 이해한다", "함수는 바깥에서 parameter로 값을 받고 내부 규칙을 수행한 뒤 return으로 결과를 돌려준다. 함수 내부에서 print만 하고 값을 돌려주지 않으면 다른 계산에서 재사용하기 어렵다. 출력과 반환을 구분하는 습관이 중요하다."),
                depthTopic("scope가 왜 버그를 만든는가", "변수는 어디서 만들었는지에 따라 사용할 수 있는 범위가 달라진다. 함수 내부 임시 변수와 프로그램 전체에서 공유하는 상태를 구분하지 않으면 예상치 못한 변경이 생긴다. 가능하면 필요한 값을 parameter로 받고 결과를 return하는 방식이 흐름을 추적하기 쉽다.")
            ),
            workedExample = """
                users = [
                  {name: "A", age: 20},
                  {name: "B", age: 17}
                ]

                function isAdult(age)
                  return age >= 19

                각 user를 반복
                  isAdult(user.age) 호출
                  true면 성인 목록에 추가

                데이터 묶기 + 반복 + 함수가 한 흐름으로 연결됨
            """,
            mistakes = listOf(
                "함수 안에서 화면 출력만 하고 필요한 값을 return하지 않는다.",
                "전역 변수를 여러 함수가 동시에 바꾸게 만들어 흐름을 추적하기 어렵게 한다.",
                "list와 map의 역할 차이를 모르고 모든 데이터를 한 구조에 억지로 넣는다.",
                "함수 하나가 입력 검증·DB 저장·화면 출력까지 모두 맡는다."
            ),
            questions = listOf(
                "함수 하나는 어느 정도 크기가 적당할까?",
                "list와 set과 map은 어떤 검색 방식에서 차이가 날까?",
                "함수 안에서 다른 함수를 호출하면 stack에는 무엇이 생길까?",
                "재귀 함수는 반복문과 어떤 관계가 있을까?"
            )
        ),
        depthPack(
            sectionId = "V1-C02-S04",
            topics = listOf(
                depthTopic("파일을 나누는 기준은 함수 나누기와 연결된다", "관련 함수와 데이터를 하나의 module로 묶으면 다른 곳에서 내부 세부 구현을 몰라도 기능을 사용할 수 있다. 파일을 너무 잘게 쪼개면 이동이 많아지고, 하나에 다 몰아넣으면 책임이 섞인다. 변경 이유가 비슷한 코드끼리 모으는 것이 좋은 출발점이다."),
                depthTopic("import는 단순 복사가 아니다", "module을 import한다는 것은 다른 파일이 제공하는 이름이나 기능을 현재 코드에서 사용할 수 있게 연결하는 것이다. 언어와 실행 환경에 따라 한 번만 로드해 재사용하거나 의존성 그래프를 구성한다. 순환 import가 생기면 서로가 서로를 먼저 필요로 해 초기화 문제가 생길 수 있다."),
                depthTopic("예외 처리는 실패를 숨기는 기능이 아니다", "try/catch를 쓰는 목적은 모든 오류를 무시하는 것이 아니라 예상 가능한 실패를 적절한 수준에서 처리하는 것이다. 파일 없음, 네트워크 timeout, 잘못된 사용자 입력은 각각 대응이 다르다. 아무 예외나 잡아서 빈 값으로 바꾸면 원인을 잃어 더 큰 버그가 된다."),
                depthTopic("로그와 사용자 메시지를 왜 구분하는가", "사용자에게는 ‘저장하지 못했습니다. 다시 시도하세요’처럼 행동 가능한 메시지가 필요하고, 개발자에게는 파일 경로, 상태 코드, stack trace 같은 진단 정보가 필요하다. 내부 오류 세부를 사용자에게 그대로 노출하면 혼란과 보안 문제를 만들 수 있다.")
            ),
            workedExample = """
                order/
                  price.py      → 가격 계산
                  validator.py  → 주문 입력 검증
                  repository.py → 저장

                main.py
                  입력 받기
                  validator 호출
                  price 계산
                  repository 저장

                저장 실패
                  사용자: "주문 저장에 실패했습니다"
                  개발 로그: 실제 exception + 입력 식별자 + 시각
            """,
            mistakes = listOf(
                "모든 코드를 한 파일에 두고 기능이 커져도 계속 추가한다.",
                "모든 exception을 한꺼번에 잡고 아무 일도 없던 것처럼 넘긴다.",
                "내부 stack trace와 민감한 값을 사용자 화면에 그대로 보여 준다.",
                "module 간 의존 방향을 생각하지 않아 순환 참조를 만든다."
            ),
            questions = listOf(
                "package와 module은 언어마다 어떻게 다를까?",
                "오류를 어느 계층에서 잡는 것이 가장 적절할까?",
                "로그 level은 왜 debug/info/warn/error로 나뉠까?",
                "의존성 주입은 module 연결 문제를 어떻게 줄일까?"
            )
        ),

        depthPack(
            sectionId = "V1-C03-S01",
            topics = listOf(
                depthTopic("array와 linked list의 차이는 모양보다 비용 차이다", "array 계열은 연속된 위치와 index를 이용해 특정 위치를 빠르게 찾는 데 강하다. linked list는 node가 다음 node를 가리키므로 중간 연결을 바꾸는 작업은 자연스럽지만 특정 번째 값을 찾으려면 앞에서부터 따라가야 한다. 자료구조는 이름보다 어떤 작업이 자주 일어나는지를 보고 선택한다."),
                depthTopic("stack과 queue가 실제 앱 어디에 쓰이는가", "stack은 마지막에 들어온 작업을 먼저 꺼내므로 뒤로 가기 기록, 함수 호출, undo 처리와 연결된다. queue는 먼저 들어온 작업을 먼저 처리하므로 작업 대기열, 메시지 처리, BFS 탐색에 자연스럽다. 같은 데이터라도 꺼내는 순서가 프로그램 동작을 바꾼다."),
                depthTopic("hash table이 빠르다고 항상 O(1)은 아니다", "hash table은 key를 hash 값으로 바꿔 위치 후보를 빠르게 찾는다. 하지만 서로 다른 key가 같은 위치를 원하면 collision 처리가 필요하고, 데이터가 너무 차면 확장 비용도 생긴다. 그래서 평균적인 빠름과 최악의 경우를 구분해서 이해한다."),
                depthTopic("reference를 이해하면 자료구조 그림이 실제 코드로 연결된다", "linked list의 next, tree의 left/right는 다른 node 자체를 복사해 넣는 것이 아니라 그 node를 찾아갈 수 있는 참조를 보관한다고 생각하면 쉽다. 참조가 잘못 연결되거나 끊기면 데이터가 사라진 것처럼 보이거나 cycle이 생길 수 있다."),
                depthTopic("자료구조는 메모리 사용량도 함께 본다", "빠른 검색을 위해 index나 hash bucket 같은 추가 정보를 저장하면 메모리를 더 쓴다. 반대로 메모리를 적게 쓰려고 압축하면 읽는 계산이 늘 수 있다. 실제 시스템은 시간과 메모리 사이에서 균형을 잡는다.")
            ),
            workedExample = """
                작업: 주문 번호로 주문을 자주 찾는다

                list만 사용
                  주문 1부터 순서대로 비교
                  최악에는 끝까지 확인

                hash map 사용
                  key = 주문 번호
                  hash 계산
                  후보 위치에서 바로 찾기

                대신 hash 구조를 유지할 추가 메모리가 필요함
            """,
            mistakes = listOf(
                "array는 무조건 빠르고 linked list는 무조건 느리다고 외운다.",
                "stack과 queue를 단순 용어로만 외우고 꺼내는 순서를 추적하지 않는다.",
                "hash table의 평균 O(1)을 어떤 상황에서도 정확한 상수 시간으로 생각한다.",
                "node 연결을 값 복사와 reference 연결로 구분하지 않는다.",
                "성능을 시간만 보고 메모리 비용은 전혀 보지 않는다."
            ),
            questions = listOf(
                "dynamic array가 가득 차면 내부에서는 어떻게 늘어날까?",
                "hash collision은 어떤 방식으로 해결할 수 있을까?",
                "deque는 stack과 queue를 어떻게 함께 표현할까?",
                "garbage collector는 더 이상 참조되지 않는 node를 어떻게 처리할까?"
            )
        ),
        depthPack(
            sectionId = "V1-C03-S02",
            topics = listOf(
                depthTopic("tree는 왜 계층 데이터에 자연스러운가", "폴더, 조직도, 댓글 답글처럼 한 항목 아래에 여러 자식이 생기는 데이터는 tree로 표현하면 부모-자식 관계가 구조 자체에 드러난다. 다만 현실 데이터가 항상 tree인 것은 아니다. 한 node가 여러 부모와 연결될 수 있으면 graph가 더 자연스러울 수 있다."),
                depthTopic("BST가 빠르려면 왜 균형이 중요할까", "BST는 작은 값은 왼쪽, 큰 값은 오른쪽이라는 규칙으로 탐색 범위를 줄인다. 하지만 값이 정렬된 순서로 계속 들어와 한쪽으로만 길어지면 사실상 linked list처럼 돼 탐색 이점이 줄어든다. 균형 tree가 필요한 이유가 여기서 나온다."),
                depthTopic("heap은 정렬된 tree가 아니다", "heap은 부모가 자식보다 우선순위가 높거나 낮다는 규칙만 보장한다. 전체 요소가 왼쪽부터 완전히 정렬돼 있다는 뜻은 아니다. 그래서 가장 큰 값이나 작은 값을 빠르게 꺼내는 priority queue에는 좋지만 임의의 값을 빠르게 검색하는 구조는 아니다."),
                depthTopic("trie가 자동완성에 어울리는 이유", "apple, app, apply처럼 앞부분이 같은 문자열을 각 단어마다 통째로 비교하지 않고 공통 prefix 경로를 공유할 수 있다. 사용자가 app까지 입력했을 때 그 node 아래를 탐색하면 후보를 찾기 쉽다. 대신 문자 node가 많이 생겨 메모리 사용이 커질 수 있다.")
            ),
            workedExample = """
                BST에 8, 3, 10, 1, 6 저장

                        8
                      /   \
                     3    10
                    / \
                   1   6

                6 찾기
                6 < 8 → 왼쪽
                6 > 3 → 오른쪽
                6 발견

                같은 값이 1,3,6,8,10 순서로 들어오면 한쪽으로 기울 수 있음
            """,
            mistakes = listOf(
                "binary tree와 binary search tree를 같은 말로 사용한다.",
                "BST는 어떤 입력 순서에서도 항상 log 시간이라고 생각한다.",
                "heap 내부 배열이 전체 정렬돼 있다고 생각한다.",
                "trie는 문자열 검색에서 언제나 hash map보다 낫다고 단정한다."
            ),
            questions = listOf(
                "AVL과 Red-Black tree는 균형을 어떻게 유지할까?",
                "heap을 배열로 저장해도 tree 관계를 표현할 수 있는 이유는 무엇일까?",
                "파일 시스템 tree에는 symbolic link 때문에 graph 성질이 생길 수 있을까?",
                "trie 메모리를 줄이기 위한 압축 방식은 무엇이 있을까?"
            )
        ),
        depthPack(
            sectionId = "V1-C03-S03",
            topics = listOf(
                depthTopic("graph를 저장하는 두 대표 방식", "adjacency list는 각 node마다 연결된 이웃 목록을 두기 때문에 연결이 드문 graph에서 메모리를 아끼기 좋다. adjacency matrix는 모든 node 쌍의 연결 여부를 표처럼 저장해 두 node 연결 여부를 바로 확인하기 쉽지만 node 수가 커지면 공간을 많이 쓴다."),
                depthTopic("cycle 때문에 visited가 필요한 이유", "tree는 보통 부모 쪽으로 되돌아가지 않도록 구조가 정해져 있지만 graph는 A→B→C→A처럼 cycle이 가능하다. 방문 기록 없이 이웃을 계속 따라가면 같은 node를 무한히 다시 처리할 수 있다. visited는 중복 작업과 무한 반복을 막는 핵심 상태다."),
                depthTopic("binary search는 정렬이 전제다", "가운데 값을 보고 왼쪽 절반이나 오른쪽 절반을 버릴 수 있는 이유는 값들이 순서대로 정렬돼 있기 때문이다. 정렬되지 않은 데이터에서 같은 알고리즘을 쓰면 버린 절반에 정답이 있을 수 있다. 빠른 알고리즘에는 보통 전제 조건이 있다."),
                depthTopic("정렬 알고리즘을 결과만 보고 비교하면 안 된다", "모든 정렬 알고리즘은 최종적으로 순서를 만들지만 비교 횟수, 추가 메모리, 이미 정렬된 입력에서의 동작, 같은 값 순서 보존 여부가 다르다. 실제 언어의 sort 함수도 이런 조건을 고려해 구현이 선택된다."),
                depthTopic("divide and conquer는 코드를 나누는 것과 다르다", "문제를 독립적인 작은 문제로 나누고 각각 해결한 뒤 결과를 합치는 알고리즘 전략이다. merge sort는 배열을 반으로 나누고 각 절반을 정렬한 뒤 합친다. 단순히 함수를 여러 개 만든다는 의미와 구분해야 한다.")
            ),
            workedExample = """
                graph
                A -- B -- C
                |    |    |
                D -- E -- A

                A에서 탐색 시작
                visited = {A}
                B 방문 → visited = {A,B}
                C 방문 → C의 이웃 A 발견
                A는 이미 visited → 다시 들어가지 않음

                visited가 없으면 A→B→C→A 반복 가능
            """,
            mistakes = listOf(
                "graph가 있으면 무조건 adjacency matrix로 저장한다.",
                "cycle 가능성을 생각하지 않고 visited 없이 DFS/BFS를 작성한다.",
                "정렬되지 않은 배열에 binary search를 적용한다.",
                "정렬 알고리즘은 속도 하나만 비교하면 된다고 생각한다.",
                "divide and conquer를 단순 코드 분리와 같은 개념으로 이해한다."
            ),
            questions = listOf(
                "directed graph와 undirected graph 저장은 무엇이 다를까?",
                "weighted edge가 있으면 최단 경로 문제는 어떻게 달라질까?",
                "stable sort가 필요한 실제 예는 무엇일까?",
                "topological sort는 어떤 graph에서만 가능할까?"
            )
        ),
        depthPack(
            sectionId = "V1-C03-S04",
            topics = listOf(
                depthTopic("Big-O는 실제 실행 시간을 초 단위로 말하는 게 아니다", "Big-O는 입력 크기가 커질 때 작업량이 어떤 속도로 늘어나는지 보는 도구다. O(n) 코드가 작은 입력에서는 O(log n) 코드보다 빠를 수도 있다. 상수 비용과 하드웨어 차이를 버리고 성장 형태에 집중하기 때문에 대규모 입력에서 구조적인 차이를 비교하기 좋다."),
                depthTopic("재귀가 끝나려면 두 조건이 필요하다", "base case만 존재한다고 안전한 것이 아니다. 매 호출이 실제로 base case 쪽으로 가까워져야 한다. n을 줄여야 하는데 그대로 넘기면 종료 조건이 있어도 영원히 도달하지 못해 stack overflow가 생길 수 있다."),
                depthTopic("BFS와 DFS는 자료구조 선택이 탐색 순서를 만든다", "BFS는 queue를 사용해 가까운 단계부터 넓게 탐색하고, DFS는 stack이나 재귀를 사용해 한 경로를 깊게 들어간다. 둘 다 모든 node를 방문할 수 있지만 먼저 만나는 순서가 다르므로 최단 단계 탐색이나 경로 탐색 같은 목적에 따라 선택한다."),
                depthTopic("greedy가 맞으려면 근거가 필요하다", "매 순간 가장 좋아 보이는 선택을 한다고 전체 최적해가 자동으로 나오지 않는다. 동전 단위나 일정 선택처럼 greedy가 성립하는 구조가 있어야 한다. 반례를 하나 만들 수 있다면 현재 greedy 규칙은 안전하지 않을 수 있다."),
                depthTopic("DP는 어려운 표가 아니라 반복 계산 제거에서 시작한다", "재귀로 같은 작은 문제를 계속 계산한다면 결과를 저장해 다시 쓰는 memoization을 생각할 수 있다. 어떤 상태를 key로 저장해야 하는지, 이전 작은 답으로 다음 답을 어떻게 만드는지가 DP의 핵심이다. 공식을 외우기보다 중복 호출 그림부터 보는 편이 좋다.")
            ),
            workedExample = """
                Fibonacci(5)
                  F(4) + F(3)
                  F(4) 안에서도 F(3) 계산
                  같은 F(3)을 반복 계산

                memo = {}
                F(3)을 처음 계산 → memo[3] = 2
                다음 F(3) 요청 → 다시 계산하지 않고 2 사용

                핵심: 같은 상태의 답을 저장해 중복 작업 제거
            """,
            mistakes = listOf(
                "Big-O를 실제 밀리초와 같은 정확한 실행 시간으로 읽는다.",
                "재귀 함수에 base case만 있으면 반드시 종료된다고 생각한다.",
                "BFS와 DFS 이름만 외우고 queue/stack에 따라 순서가 바뀌는 이유를 모른다.",
                "greedy는 가장 큰 값부터 고르면 된다는 식으로 일반화한다.",
                "DP 문제는 점화식부터 외우고 어떤 상태가 반복되는지 보지 않는다."
            ),
            questions = listOf(
                "O(n log n)은 어떤 알고리즘에서 자주 나타날까?",
                "recursion depth가 너무 깊을 때 반복문으로 바꾸면 무엇이 달라질까?",
                "BFS가 unweighted graph 최단 거리에서 유용한 이유는 무엇일까?",
                "memoization과 tabulation은 어떤 차이가 있을까?"
            )
        )
    )
}
