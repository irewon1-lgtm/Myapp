package com.futuretech.poweruser.data

internal object V2TrackPracticeData {
    private fun t(
        n: Int,
        title: String,
        explanation: String,
        outcome: String,
        language: String,
        code: String,
        practice: String,
        fill: String,
        broken: String,
        fix: String,
        question: String,
        options: List<String>,
        answer: Int,
        keywords: List<String>,
        minutes: Int = 45
    ) = LessonSeed(
        id = "V2-T%02d".format(n),
        type = "TEXTBOOK_V2",
        sequence = n,
        module = 2,
        moduleTitle = "코딩 완전과정",
        step = n,
        title = title,
        explanation = explanation,
        outcome = outcome,
        language = language,
        code = code,
        practice = practice,
        fill = fill,
        broken = broken,
        fix = fix,
        aiQuestion = question,
        aiOptions = options,
        aiAnswer = answer,
        keywords = keywords,
        minutes = minutes,
        stepTotal = 11
    )

    val lessons: List<LessonContent> = listOf(
        t(
            1,
            "컴퓨터와 프로그래밍의 언어",
            "입력→처리→출력, 값·타입·변수, 파일·실행·오류를 직접 코드로 확인합니다. 실행된다는 것과 정답이라는 것을 구분하고 작은 오류를 증거로 고칩니다.",
            "간단한 계산 프로그램을 읽고 예상한 뒤 실행하고, 논리 오류 한 줄을 찾아 최소 수정한다.",
            "PYTHON",
            "price = 2500\ncount = 4\ntotal = price * count\nprint(total)",
            "price = 3000\ncount = 5\ntotal = price * count\nprint(total)",
            "프로그램이 현재 기억하고 있는 값들의 상황을 [ 상태 / state ]라고 합니다.",
            "price = 2500\ncount = 4\ntotal = price + count\nprint(total)",
            "price = 2500\ncount = 4\ntotal = price * count\nprint(total)",
            "코드가 오류 없이 실행되면 그 결과는 항상 의도한 정답입니까?",
            listOf("예. 실행 성공이면 논리도 항상 맞다.", "아니다. 실행은 되지만 계산 규칙이 틀린 논리 오류가 있을 수 있다.", "숫자를 쓰는 프로그램에서만 맞다."),
            1,
            listOf("입력", "처리", "출력", "상태", "논리 오류")
        ),
        t(
            2,
            "프로그래밍 사고와 문법",
            "값·변수·조건·반복·함수·스코프·예외를 실제 작은 프로그램으로 묶습니다. 큰 문제를 입력·처리·출력으로 나누고 함수마다 한 역할을 갖게 합니다.",
            "조건과 함수를 사용해 할인 계산 로직을 직접 작성하고 경계값을 실행한다.",
            "PYTHON",
            "def shipping(total):\n    if total >= 10000:\n        return 'free'\n    return '3000'\n\nprint(shipping(12000))",
            "def shipping(total):\n    if total >= 20000:\n        return 'free'\n    return '3000'\n\nprint(shipping(25000))",
            "함수가 결과를 호출한 곳으로 돌려주는 문법은 [ return ]입니다.",
            "def grade(score):\n    if score >= 90:\n        return 'A'\n    if score >= 80:\n        return 'C'\n    return 'C'\n\nprint(grade(85))",
            "def grade(score):\n    if score >= 90:\n        return 'A'\n    if score >= 80:\n        return 'B'\n    return 'C'\n\nprint(grade(85))",
            "함수 안에서 만든 지역 변수를 모든 코드에서 전역 변수처럼 사용할 수 있습니까?",
            listOf("항상 가능하다.", "아니다. 변수는 스코프에 따라 보이는 범위가 다르다.", "문자열 변수만 가능하다."),
            1,
            listOf("조건", "반복", "함수", "return", "scope")
        ),
        t(
            3,
            "자료구조와 알고리즘",
            "자료구조를 이름으로 외우지 않고 검색·삽입·삭제·순서 요구에 따라 선택합니다. 선형 탐색과 이진 탐색, queue와 set을 실행하며 복잡도와 경계값을 확인합니다.",
            "목록 검색 함수를 직접 작성하고 빈 목록·없는 값·첫 값·마지막 값에서 결과를 검증한다.",
            "PYTHON",
            "def find(items, target):\n    for index, item in enumerate(items):\n        if item == target:\n            return index\n    return -1\n\nprint(find([3, 7, 9, 12], 9))",
            "def find(items, target):\n    for index, item in enumerate(items):\n        if item == target:\n            return index\n    return -1\n\nprint(find([1, 4, 8, 15], 15))",
            "정렬된 데이터의 검색 범위를 절반씩 줄이는 탐색을 [ 이진 탐색 / binary search ]이라고 합니다.",
            "def find(items, target):\n    for index, item in enumerate(items):\n        if item != target:\n            return index\n    return -1\n\nprint(find([3, 7, 9], 9))",
            "def find(items, target):\n    for index, item in enumerate(items):\n        if item == target:\n            return index\n    return -1\n\nprint(find([3, 7, 9], 9))",
            "BFS에서 이미 방문한 노드를 기록하지 않으면 어떤 문제가 생길 수 있습니까?",
            listOf("그래프의 cycle에서 같은 노드를 반복 방문할 수 있다.", "배열이 자동 정렬된다.", "시간복잡도가 항상 O(1)이 된다."),
            0,
            listOf("자료구조", "탐색", "Big-O", "BFS", "경계값")
        ),
        t(
            4,
            "웹 화면과 브라우저",
            "HTML 구조·CSS 배치·DOM 이벤트를 하나의 화면으로 연결합니다. 시맨틱 요소와 접근성, 360px 반응형, 버튼 event를 실제 HTML/JS preview에서 확인합니다.",
            "시맨틱 HTML과 button event를 사용해 작은 학습 카드 UI를 직접 수정한다.",
            "HTML_JS",
            "<!doctype html>\n<html lang=\"ko\"><body>\n<h1>TRACK</h1>\n<button id=\"next\">다음 LESSON</button>\n<script>document.querySelector('#next').addEventListener('click', () => { document.querySelector('h1').textContent = '완료'; });</script>\n</body></html>",
            "<!doctype html>\n<html lang=\"ko\"><body>\n<main><h1>학습 카드</h1><p>본문</p><button id=\"next\">다음 LESSON</button></main>\n<script>document.querySelector('#next').addEventListener('click', () => { document.querySelector('p').textContent = '다음으로 이동'; });</script>\n</body></html>",
            "HTML 문서를 JavaScript가 읽고 바꿀 수 있는 객체 트리 구조를 [ DOM ]이라고 합니다.",
            "<!doctype html>\n<html><body><button id=\"save\">저장</button><script>document.querySelector('#wrong').addEventListener('click', () => console.log('saved'));</script></body></html>",
            "<!doctype html>\n<html><body><button id=\"save\">저장</button><script>document.querySelector('#save').addEventListener('click', () => console.log('saved'));</script></body></html>",
            "클릭 행동을 만들 때 의미 없는 div를 button처럼 꾸미는 것과 실제 button을 사용하는 것 중 기본 접근성에 더 적합한 것은?",
            listOf("실제 button 요소", "항상 div", "둘은 브라우저에서 완전히 같은 의미"),
            0,
            listOf("HTML", "CSS", "DOM", "event", "accessibility")
        ),
        t(
            5,
            "JavaScript와 TypeScript 깊게",
            "JavaScript의 참조·closure·Promise·async/await·event loop와 race condition을 이해하고 TypeScript의 union·narrowing으로 실행 전 오류를 줄입니다.",
            "최신 요청만 반영하는 TypeScript 함수를 작성하고 오래된 요청 ID가 결과를 덮지 못하게 한다.",
            "TYPESCRIPT",
            "function isLatest(requestId: number, latestId: number): boolean {\n  return requestId === latestId;\n}\nconsole.log(isLatest(3, 3));",
            "function isLatest(requestId: number, latestId: number): boolean {\n  return requestId === latestId;\n}\nconsole.log(isLatest(5, 5));",
            "Promise의 then이나 await 이후 이어질 작업이 기다리는 높은 우선순위 대기 종류를 [ microtask / 마이크로태스크 ]라고 합니다.",
            "function isLatest(requestId: number, latestId: number): boolean {\n  return requestId <= latestId;\n}\nconsole.log(isLatest(2, 3));",
            "function isLatest(requestId: number, latestId: number): boolean {\n  return requestId === latestId;\n}\nconsole.log(isLatest(2, 3));",
            "async 함수 앞에 async를 붙이면 CPU 여러 개에서 자동 병렬 실행됩니까?",
            listOf("예. 항상 새 CPU core를 쓴다.", "아니다. Promise/await 흐름을 위한 문법이며 병렬성과 같은 뜻이 아니다.", "브라우저에서만 병렬이고 Node.js에서는 동기다."),
            1,
            listOf("Promise", "await", "event loop", "race condition", "narrowing")
        ),
        t(
            6,
            "인터넷·네트워크·API",
            "IP·DNS·port·HTTP의 경계를 나누고 header와 body, MIME, Content-Type, Accept, multipart boundary를 실제 데이터로 읽습니다. 네트워크 오류와 HTTP 오류를 구분합니다.",
            "HTTP 응답의 status와 Content-Type을 검사해 성공·데이터 종류를 분리해서 판정한다.",
            "PYTHON",
            "status = 200\ncontent_type = 'application/json'\nprint(status == 200 and content_type == 'application/json')",
            "status = 201\ncontent_type = 'application/json'\nprint(200 <= status < 300 and content_type == 'application/json')",
            "HTTP body에 실제로 담긴 데이터의 MIME type을 알려주는 header를 [ Content-Type ]이라고 합니다.",
            "status = 500\ncontent_type = 'application/json'\nok = status == 200 or content_type == 'application/json'\nprint(ok)",
            "status = 500\ncontent_type = 'application/json'\nok = 200 <= status < 300 and content_type == 'application/json'\nprint(ok)",
            "multipart/form-data의 boundary는 무엇을 위해 사용합니까?",
            listOf("HTTP status를 암호화한다.", "multipart body 안의 각 데이터 부분을 구분한다.", "DNS 주소를 찾는다."),
            1,
            listOf("HTTP", "MIME", "Content-Type", "multipart", "boundary")
        ),
        t(
            7,
            "서버와 백엔드",
            "route→auth→validation→service→DB/외부 API→response 흐름을 따라갑니다. retry가 중복 실행을 만들 수 있으므로 idempotency와 timeout을 함께 설계합니다.",
            "idempotency key를 이용해 같은 결제 요청을 두 번 처리하지 않는 작은 서버 로직을 작성한다.",
            "PYTHON",
            "processed = set()\n\ndef pay(key):\n    if key in processed:\n        return 'duplicate'\n    processed.add(key)\n    return 'paid'\n\nprint(pay('abc'))\nprint(pay('abc'))",
            "processed = set()\n\ndef create_order(key):\n    if key in processed:\n        return 'duplicate'\n    processed.add(key)\n    return 'created'\n\nprint(create_order('order-1'))\nprint(create_order('order-1'))",
            "같은 요청을 반복해도 최종 효과가 중복되지 않는 성질을 [ 멱등성 / idempotency ]이라고 합니다.",
            "processed = set()\n\ndef pay(key):\n    processed.add(key)\n    return 'paid'\n\nprint(pay('abc'))\nprint(pay('abc'))",
            "processed = set()\n\ndef pay(key):\n    if key in processed:\n        return 'duplicate'\n    processed.add(key)\n    return 'paid'\n\nprint(pay('abc'))\nprint(pay('abc'))",
            "400 Bad Request처럼 같은 잘못된 입력 때문에 실패한 요청을 무한 retry하는 것이 좋은 기본 전략입니까?",
            listOf("예. 모든 오류는 성공할 때까지 retry해야 한다.", "아니다. 일시 오류와 영구적인 입력 오류를 구분해야 한다.", "POST에서만 항상 맞다."),
            1,
            listOf("validation", "authorization", "idempotency", "retry", "timeout")
        ),
        t(
            8,
            "데이터베이스",
            "table·row·key·SQL·JOIN·index·constraint·transaction을 실제 SQLite sandbox에서 실행합니다. 무결성 규칙과 ACID의 의미를 데이터 변경 실패와 연결합니다.",
            "stock_practice table을 조건·정렬해 조회하고 잘못된 SQL 한 줄을 수정한다.",
            "SQL",
            "SELECT name, price FROM stock_practice ORDER BY price DESC;",
            "SELECT name, price, rating FROM stock_practice WHERE price >= 100000 ORDER BY price DESC;",
            "table의 각 row를 고유하게 식별하는 key를 [ PRIMARY KEY / 기본키 ]라고 합니다.",
            "SELECT name, price FORM stock_practice ORDER BY price DESC;",
            "SELECT name, price FROM stock_practice ORDER BY price DESC;",
            "FOREIGN KEY의 가장 중요한 역할은 무엇입니까?",
            listOf("다른 table의 존재하는 row와 참조 관계가 깨지지 않게 돕는다.", "모든 query를 자동 O(1)로 만든다.", "비밀번호를 암호화한다."),
            0,
            listOf("SQL", "PRIMARY KEY", "FOREIGN KEY", "무결성", "transaction")
        ),
        t(
            9,
            "보안과 데이터 보호",
            "encoding·hash·encryption을 구분하고 trust boundary에서 외부 입력을 검증합니다. 파일 이름·MIME 같은 client 주장을 그대로 신뢰하지 않는 기본 방어를 연습합니다.",
            "업로드 파일 이름의 위험한 경로 조각을 거부하는 검증 함수를 작성하고 우회 입력을 시험한다.",
            "PYTHON",
            "def safe_name(name):\n    return '..' not in name and '/' not in name and '\\\\' not in name\n\nprint(safe_name('photo.png'))",
            "def safe_name(name):\n    return '..' not in name and '/' not in name and '\\\\' not in name\n\nprint(safe_name('avatar.jpg'))",
            "데이터를 key로 숨기고 올바른 key로 원본을 복구할 수 있게 만드는 것을 [ 암호화 / encryption ]라고 합니다.",
            "def safe_name(name):\n    return name.endswith('.png')\n\nprint(safe_name('../../photo.png'))",
            "def safe_name(name):\n    return name.endswith('.png') and '..' not in name and '/' not in name and '\\\\' not in name\n\nprint(safe_name('../../photo.png'))",
            "Base64 encoding을 비밀번호 암호화로 사용해도 안전합니까?",
            listOf("예. 사람이 바로 못 읽으므로 암호화다.", "아니다. Base64는 되돌릴 수 있는 encoding이지 비밀 보호용 encryption이 아니다.", "HTTPS에서만 안전한 암호가 된다."),
            1,
            listOf("trust boundary", "hash", "encryption", "injection", "least privilege")
        ),
        t(
            10,
            "오류·테스트·Git·빌드·배포",
            "Expected/Actual→재현→가설→실험→수정→회귀시험의 흐름과 Git commit→CI→artifact→deployment 연결을 익힙니다. 실행하지 않은 검증은 PASS가 아닙니다.",
            "실패하는 계산 함수를 재현하고 최소 수정해 정상·경계 입력에서 다시 검증한다.",
            "PYTHON",
            "def total(price, count):\n    return price * count\n\nprint(total(2500, 4))",
            "def total(price, count):\n    return price * count\n\nprint(total(1200, 5))",
            "기존 정상 기능이 변경 후 다시 깨졌는지 확인하는 테스트를 [ 회귀 테스트 / regression test ]라고 합니다.",
            "def total(price, count):\n    return price + count\n\nprint(total(2500, 4))",
            "def total(price, count):\n    return price * count\n\nprint(total(2500, 4))",
            "코드를 읽어 보니 될 것 같지만 실제 test를 실행하지 않았습니다. 상태를 어떻게 기록해야 합니까?",
            listOf("PASS", "완료", "미실행/정적 검토 상태"),
            2,
            listOf("재현", "회귀시험", "Git", "artifact", "CLEAN PASS")
        ),
        t(
            11,
            "소프트웨어 설계와 종합 프로젝트",
            "요구사항·책임·상태 전이·불변조건·결합도·실패 경계를 설계하고 AI가 만든 구현을 구조와 테스트 증거로 검수합니다.",
            "주문 상태 머신을 작성해 허용된 전이만 통과시키고 잘못된 전이를 거부한다.",
            "PYTHON",
            "allowed = {('CREATED', 'PAID'), ('PAID', 'SHIPPED'), ('CREATED', 'CANCELLED')}\n\ndef can_move(before, after):\n    return (before, after) in allowed\n\nprint(can_move('CREATED', 'PAID'))",
            "allowed = {('CREATED', 'PAID'), ('PAID', 'SHIPPED'), ('CREATED', 'CANCELLED')}\n\ndef can_move(before, after):\n    return (before, after) in allowed\n\nprint(can_move('PAID', 'SHIPPED'))",
            "시스템에서 항상 지켜야 하는 조건을 [ 불변조건 / invariant ]이라고 합니다.",
            "allowed = {('CREATED', 'PAID')}\n\ndef can_move(before, after):\n    return before != after\n\nprint(can_move('SHIPPED', 'CREATED'))",
            "allowed = {('CREATED', 'PAID')}\n\ndef can_move(before, after):\n    return (before, after) in allowed\n\nprint(can_move('SHIPPED', 'CREATED'))",
            "좋은 architecture 선택을 설명하는 가장 적절한 방식은 무엇입니까?",
            listOf("유행하는 패턴이므로 사용했다.", "요구사항·제약·trade-off를 비교해 이 구조를 선택한 근거를 설명한다.", "파일 수가 많을수록 좋은 구조다."),
            1,
            listOf("requirement", "responsibility", "invariant", "coupling", "trade-off")
        )
    ).map { it.toLesson() }
}
