{"chapter":5,"title":"입력과 형변환","pages":[{"kind":"concept","eyebrow":"CHAPTER 05 · 입력 받기","title":"프로그램은 입력을 받아 처리하고 출력합니다","paragraphs":["프로그램을 세 칸으로 나누면 이해가 쉬워집니다. 입력(input)은 밖에서 들어오는 값, 처리(process)는 계산·변환·저장 같은 중간 작업, 출력(output)은 사용자에게 보여 주는 결과입니다.","input()은 키보드 입력을 받는 Python 함수입니다. 중요한 점은 사용자가 숫자를 입력해도 input()의 결과는 기본적으로 문자열(str)이라는 것입니다.","숫자 계산이 필요하면 int()나 float()로 바꿔야 합니다. 이런 과정을 형 변환(type conversion)이라고 합니다.","사용자 입력은 프로그램 바깥에서 들어오는 데이터입니다. 그래서 프로그램 내부에서 우리가 직접 만든 값보다 더 조심해서 다루는 편이 좋습니다. 사용자가 빈칸을 입력할 수도 있고, 숫자를 요구했는데 글자를 입력할 수도 있기 때문입니다.","입문 단계에서는 복잡한 검증까지 하지 않더라도 ‘외부 입력은 먼저 타입을 확인하고 필요한 형태로 정리한다’는 원칙을 기억하면 좋습니다. 이 원칙은 나중에 파일·웹·API 데이터를 다룰 때도 그대로 이어집니다."],"keywords":["input 입력","process 처리","output 출력","I/O 입력/출력","conversion 형 변환","return value 반환값"],"calloutTitle":"초보자가 자주 막히는 지점","calloutBody":"키보드로 10을 입력해도 처음에는 글자 \"10\"입니다. 계산하려면 숫자 타입으로 바꿔야 합니다."},{"kind":"visual","eyebrow":"데이터 흐름","title":"입력 → 타입 확인/변환 → 처리 → 출력","visual":"INPUT_CONVERSION","paragraphs":["입력 처리 문제에서는 두 질문을 하면 됩니다. ‘지금 이 값은 어떤 타입인가?’ 그리고 ‘다음 연산에는 어떤 타입이 필요한가?’입니다.","외부에서 들어오는 값은 완벽하다고 가정하지 않는 습관이 중요합니다. 지금은 int 변환만 배우지만, 나중에는 빈 값·범위·잘못된 형식도 검사하게 됩니다.","입력 → 변환 → 처리 → 출력 흐름을 한 줄에 모두 적을 수도 있지만, 초보 단계에서는 중간값을 변수로 나누는 편이 훨씬 읽기 쉽습니다. raw_age = input(...), age = int(raw_age)처럼 단계가 보이면 어느 부분에서 문제가 났는지도 찾기 쉽습니다.","실무에서도 외부 데이터는 경계(boundary)에서 가능한 한 일찍 정리하고 검증하는 편이 좋습니다. 지금은 작은 콘솔 프로그램이지만 사고방식은 같습니다."],"bullets":["input() 결과는 str","계산이 필요하면 int() 또는 float()","변환이 실패하면 ValueError 가능","처리 후 print 또는 f-string으로 출력","외부에서 들어온 값은 바로 믿지 않고 먼저 확인합니다.","변환과 계산을 분리하면 오류 위치가 더 잘 보입니다."]},{"kind":"example","eyebrow":"예제","title":"입력받은 나이로 다음 나이를 계산합니다","code":"name = input(\"이름: \")\nage = int(input(\"나이: \"))\nnext_age = age + 1\nprint(f\"{name}님의 다음 나이는 {next_age}살입니다\")","paragraphs":["name은 글자로 사용하므로 str 그대로 두고, age는 + 1 계산이 필요하므로 int로 변환합니다.","f-string은 문자열 안에 변수 값을 넣기 쉽게 해 줍니다. 중괄호 안의 변수 값이 실제 값으로 바뀌어 출력됩니다.","int(input(...))처럼 한 줄로 쓰면 짧지만, 처음에는 두 단계로 나눠 보는 것도 좋습니다. raw_age = input(\"나이: \")로 먼저 받은 뒤 print(type(raw_age))를 확인하고, age = int(raw_age)로 변환하면 과정이 눈에 보입니다.","f-string은 여러 변수 값을 자연스럽게 문장에 넣을 때 편리합니다. 문자열 앞에 f를 붙이고 중괄호 안에 변수 이름이나 간단한 표현식을 적으면 해당 값이 문자열 안에 들어갑니다."]},{"kind":"practice","eyebrow":"실습 A/B · 입력받는 계산기","title":"숫자 입력을 계산에 사용해 보세요","practicePrompt":"① 숫자 두 개를 input()으로 받기 ② int로 변환해 더하기 ③ 결과 출력 ④ int를 일부러 빼고 결과가 어떻게 달라지는지 확인","code":"a = int(input(\"첫 수: \"))\nb = int(input(\"둘째 수: \"))\nprint(a + b)","answer":"int를 빼면 두 입력은 문자열입니다. 예를 들어 10과 5를 입력하면 숫자 15가 아니라 문자열 연결 결과 105가 나올 수 있습니다.","paragraphs":["int를 빼고 문자열 두 개를 +하면 숫자 덧셈이 아니라 문자열 연결이 일어납니다. 이 차이를 직접 경험해 보면 ‘입력은 문자열’이라는 사실이 훨씬 오래 기억됩니다.","실습할 때 10과 5뿐 아니라 0, 음수, 큰 수 등 여러 값을 넣어 보세요. 입력값이 바뀌어도 프로그램의 구조는 그대로 유지되어야 합니다."]},{"kind":"check","eyebrow":"확인 문제","title":"입력과 형 변환","question":"x = input(\"숫자: \") 뒤에 7을 입력했습니다. x의 타입은 무엇이고, x + 3을 계산하려면 무엇이 필요할까요?","answer":"x는 str입니다. 숫자 계산을 하려면 int(x) 또는 상황에 따라 float(x)로 변환해야 합니다.","mistake":"‘숫자를 입력했으니 자동으로 숫자 타입이 되겠지’라고 생각하기 쉽지만 input()은 기본적으로 문자열을 반환합니다.","paragraphs":["x + 3에서 x가 str이라면 TypeError가 발생할 수 있습니다. 반대로 int(\"사과\")처럼 문자열을 숫자로 바꾸려고 했지만 내용이 숫자가 아니면 ValueError가 발생합니다. 둘 다 ‘입력 문제’처럼 보이지만 원인 종류가 다릅니다.","오류 이름을 정확히 구분하면 검색이나 AI 질문도 더 구체적으로 할 수 있습니다. ‘Python input 숫자 계산 안 됨’보다 ‘input 결과 str이라 int 변환 후 ValueError’라고 설명하는 편이 훨씬 유용합니다."]},{"kind":"summary","eyebrow":"Chapter 5 정리","title":"입력은 타입까지 확인합니다","paragraphs":["프로그램은 입력을 받고, 처리하고, 결과를 출력합니다. input()의 결과는 문자열이므로 숫자 계산이 필요하면 int()나 float()로 변환합니다.","입력값을 받은 즉시 필요한 형식으로 정리해 두면 뒤의 코드가 단순해집니다. 잘못된 값은 변환 과정에서 ValueError를 일으킬 수 있다는 점도 기억하세요.","다음 챕터에서는 이런 오류 메시지를 읽고 원인을 좁히는 디버깅 방법을 배웁니다.","입력을 다룰 때는 값 자체뿐 아니라 타입과 형식을 함께 봅니다. 프로그램 내부로 들어온 값이 이후 계산이나 출력에 맞는 형태인지 확인하는 습관은 매우 중요합니다.","다음 챕터에서는 변환 실패나 잘못된 변수 이름처럼 실제로 자주 만나는 오류를 읽는 방법을 배웁니다. 입력과 타입을 이해해 두면 오류 메시지의 의미도 더 잘 보이기 시작합니다."],"bullets":["input() → str","계산 전 필요한 타입으로 변환","변환 실패도 정상적으로 만날 수 있는 오류","입력→처리→출력 구조로 생각하기","input()은 사용자와 프로그램 사이의 경계다.","외부 입력은 필요한 타입으로 정리한 뒤 사용한다.","TypeError와 ValueError의 차이를 상황과 함께 본다."]}]}