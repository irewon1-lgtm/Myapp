# 챗북 기본 교재 엔진 1.0

대상: `irewon1-lgtm/Myapp`, **chatbook-app-20260923만 사용**. main 및 다른 앱을 변경하지 않는다.
운영 사이트: https://chatbook-library-20260923.netlify.app

## 실제 구현 범위

`public/engine/core.mjs`는 Node 22+와 HTTPS 브라우저에서 실행되는 제작 통제 엔진이다.
원문 파싱/정리, 입력 한도에 따른 구간 분할, 2단계 집필용 작업 묶음, 근거 및 누락 검사,
독립 검수 결과 연결, 그림 출처 검사, 게시 승인, 기존 챗북 JSON 생성 기능이 있다.
`engine/runner.mjs`는 주입된 작성 모델을 동시 2작업으로 실행하며 작업별 상태를 저장한다.
`engine/cli.mjs`는 파일 기반 작업을 만들고 재개한다. `engine/frames.py`는 확보된 로컬 영상에서 실제 프레임을 추출한다.

**이 버전은 모든 YouTube 링크를 자동 다운로드하는 서비스가 아니다.**
작성과 의미 검수에는 ChatGPT 작성자 또는 별도 주입한 모델 제공자가 필요하다.
추가 모델/API 연결을 만들지 않았으며 외부 자동 호출은 꺼져 있다. 모델 제공자 인터페이스는 구현되어 있지만
새로운 실제 모델 API 호출은 이 설치에서 수행하지 않았다. 공급자의 과금 단가는 추측하지 않는다.

## 앞으로의 기본 작업 순서

1. 확보한 원문을 수정하지 않고 SHA-256과 함께 보존한다. SRT/VTT/JSON3 또는 시각 표시 전사를 받는다.
   시간 정렬이 없는 자료, 오류 HTML, 다른 영상의 전사, 너무 큰 단일 전사 구간은 중단한다.
2. 기본 4분 안팎, 최대 6분 및 원문 16,000 UTF-8 바이트 안에서 나눈다. 경계 원문 1구간은 참고용으로만 전달한다.
   자막의 정확한 시간 겹침이 입증된 중복만 기계적으로 제거한다. 말의 반복과 강조는 자동 삭제하지 않는다.
3. 1차 원문 기준본: 주장/정의/예시/비유/연결 설명/주의사항을 각각 의미 단위로 보존한다.
   각 의미 단위는 실제 원문 부분 인용과 구간 ID를 가진다. 광고/인사/정확한 중복 이외 누락은 허용하지 않는다.
4. 2차 초보자 해설: 기준본의 의미 단위를 모두 유지하며 표현만 풀어 쓴다.
   코딩 지식이 전혀 없는 성인 독자에게 존중하는 문체를 쓴다. 어려운 단어는 뜻→필요성→원문 예시→현재 맥락으로 설명한다.
   새 주장, 수치, 추천, 인과관계를 만들지 않는다. 원문이 틀렸거나 상충하면 몰래 수정하지 말고 검토 상태로 남긴다.
5. 다른 검수 역할이 원문/기준본/해설본을 대조한다. 근거 ID만 있는 것은 의미 검증이 아니다.
   검수 결과는 원문·기준본·해설본 해시와 연결한다. 내용 수정 시 그 구간의 검수만 무효화한다.
6. 화면: 실제 영상의 넓은 구간을 저비용 후보로 훑고, 필요한 곳을 원해상도로 확인한다.
   `hq1/hq2/hq3/hqdefault`는 강의 캡처로 금지한다. 후보가 나왔다는 이유로 자동 채택하지 않는다.
   실제 화면은 영상 지문/시각/파일 지문/읽기 가능 여부/해당 문단 관련성/얼굴 전용 아님 검토가 있어야 한다.
   재구성 그림은 `개념도`로 구분한다. 실제 화면을 못 본 경우 화면 전체 검수 완료로 표시하지 않는다.
7. 게시 전 검토 통과 + 최종 승인 필요. 검토한 결과가 바뀌면 재승인한다. 기존 교재는 실패 결과로 덮어쓰지 않는다.
   `exportBook`은 챗북 호환 JSON을 반환하고 `mergeCatalog`는 기대 버전이 맞을 때만 기존 서재에 합친다.
   운영 카탈로그 수정은 최신 Git SHA를 다시 읽고 한 번의 Git tree/commit으로 반영한다.
   새로운 이미지 파일도 JSON과 함께 먼저 올려야 하며, 실제 자산 URL 응답/모바일 표시를 확인한 뒤 최종 공개한다.

## 비용/실패 처리

- 병렬 2개, 최대 4개. 단위 작업은 최초+추가 1회까지만 허용한다.
- 401/403/로그인 필요: 중지. 429/503: Retry-After를 지킨 뒤 해당 작업 1회만 재시도.
- 타임아웃/연결 종료: 이미 처리·청구됐을 수 있으므로 UNCERTAIN_CALL로 보류한다. 중복 과금을 막기 위해 무조건 재전송하지 않는다.
- 호출 전에 최대 비용 및 출력량을 예약하고 기록한다. 잔여 예산이 부족하면 호출하지 않는다.
- 기본 추가 지출 0달러, 자동 외부 모델 호출 OFF. 사용자의 구독과 별도 API 사용료를 혼동하지 않는다.
- 저장 파일은 임시 파일→fsync→원자적 rename, 쓰기 잠금, SHA-256 검증을 사용한다.
- FileStore는 지속 저장 공간이 필요하다. Netlify 함수의 임시 디스크만으로 영구 재개를 구현했다고 주장하지 않는다.
- Netlify 동기 함수 안에서 1시간 영상 전체 작업을 실행하지 않는다. 이 배포는 새 장시간 생성 API를 공개하지 않는다.

## 실행

```sh
node --test tests/engine/*.test.mjs
node engine/benchmark.mjs
python tests/engine/frames_test.py
node engine/cli.mjs prepare /private/job-backend exact_source/0SGfDKMLdaI/transcript.md 0SGfDKMLdaI
node engine/cli.mjs task /private/job-backend c001 source
node engine/cli.mjs submit /private/job-backend c001 source source.json assistant-author
# 다음 beginner, review 단계를 같은 방식으로 수행. 의미 검수는 실제로 수행한 사람/역할만 승인한다.
node engine/cli.mjs approve /private/job-backend authorized-owner
node engine/cli.mjs export /private/job-backend book.json
```

## 검증 결과 해석

`benchmark.json`의 긴 입력 테스트는 합성 13/30/60/120분 자막을 사용한다.
스케줄 모델은 64조건×100시드=6,400회이며 대역폭/속도 가정을 파일에 명시한다.
실제 영상 재생 속도, 실제 모델 생성시간, 청구액, 의미 충실도, 사람의 이해도를 측정한 결과가 아니다.
기존 원문 3개 재생시험은 해당 repository에 원문이 존재할 때만 실행하며 실제 처리한 길이와 해시를 남긴다.
로컬 제어 영상 프레임 시험은 프레임 캡처 자체를 검사할 뿐 YouTube 접근이나 교육적 화면 선정 성공을 증명하지 않는다.
기존 책에 원문 단위 ID가 없다면 LEGACY_UNAUDITED로 취급하고 새 엔진 검수를 통과했다고 재분류하지 않는다.
